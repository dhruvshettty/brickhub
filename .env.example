# === REQUIRED ===
# Get your key at console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Pick any secure password for your local Postgres instance
POSTGRES_PASSWORD=changeme_local_only

# === OPTIONAL (Milestone 2: Suunto watch sync) ===
# Register at apizone.suunto.com → My Apps → Create App
SUUNTO_CLIENT_ID=
SUUNTO_CLIENT_SECRET=
SUUNTO_REDIRECT_URI=http://localhost:8000/api/v1/sync/suunto/callback

# === OPTIONAL (future: multi-user / production) ===
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
